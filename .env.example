# Local secrets. Copy to .env.local.

# One of these is enough for live holdings. Alchemy is preferred.
ALCHEMY_API_KEY=
ETHERSCAN_API_KEY=

# Optional. Injected browser wallets work without this.
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=
